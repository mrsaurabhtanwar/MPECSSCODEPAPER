% ==========================================================================
% Section 5 -- Numerical Experiments
% ==========================================================================
% File: section_numerical.tex
% Status: MPECLib and MacMPEC tables regenerated directly from archived
%         CSVs in results/outputs_official_*.
%         NOSBENCH retained as a manuscript placeholder pending corrected
%         full-suite archive refresh after exporter deduplication.
% Last revised: April 2026
%
% INTEGRATION NOTE FOR main.tex
% -----------------------------------------------------------
%  1. Add the following packages to the preamble (if not already present):
%       \usepackage{booktabs}
%       \usepackage{rotating}     % for sidewaystable (large tables)
%       \usepackage{multirow}
%       \usepackage{xcolor}
%       \definecolor{darkgreen}{rgb}{0.0,0.45,0.0}
%  2. Copy the two EPS figures produced by the benchmark analysis script
%     into the figures/ subdirectory:
%       mpeclib_outcomes.eps
%       mpeclib_timing.eps
%       mpeclib_comp_res.eps
%  3. Insert \input{section_numerical} before \section*{Acknowledgements}.
% ==========================================================================

\section{Numerical Experiments}
\label{sec:numerical}

This section reports the numerical performance of MPECSS across three
major MPEC benchmark collections. Section~\ref{sec:exp_setup} describes the experimental
environment and algorithm configuration.  Section~\ref{sec:mpeclib}
presents the complete MPECLib results.  Sections~\ref{sec:macmpec}
and~\ref{sec:nosbench} report the MacMPEC and NOSBENCH results
respectively.
Section~\ref{sec:perf_profile} has the Dolan--Mor\'{e}
performance profile analysis~\cite{DolanMore2002} on
MacMPEC benchmark suite.


% --------------------------------------------------------------------------
\subsection{Experimental Setup}
\label{sec:exp_setup}
% --------------------------------------------------------------------------

All benchmark runs use the same environment on the cloud service on the kaggle. Used machine is Linux on an Intel Xeon platform with 31.35\,GB RAM
under Python~3.12.12, CasADi~3.7.2, and IPOPT~\cite{WachterBiegler2006}
with the open-source linear solver MUMPS~\cite{Amestoy2001}. The code
base also contains an SQP$+$qpOASES~\cite{Ferreau2014qpOASES} route for
small problems ($n \leq 400$).

The benchmark-runner configuration used throughout all benchmarks is
listed in Table~\ref{tab:params}. A per-problem wall-clock time limit
of 3600\,s is enforced via process-level isolation (using
\texttt{multiprocessing.Process}); per-problem timeouts are enforced by
monitoring the child process rather than POSIX signals, ensuring
reliable enforcement even during C++ library (CasADi/IPOPT)
execution.\footnote{%
  To ensure single-threaded, deterministic execution, the environment
  variables \texttt{OMP\_NUM\_THREADS}, \texttt{MKL\_NUM\_THREADS},
  \texttt{OPENBLAS\_NUM\_THREADS}, and \texttt{NUMEXPR\_NUM\_THREADS}
  are all set to~1. On Windows, process-level isolation via \texttt{multiprocessing.Process} with spawn context is used since signal-based timeouts cannot interrupt blocking IPOPT calls.}
Problems that exceed this limit are recorded as \emph{timeout}.
The benchmark runner is executed with random seed~42 for
reproducibility; when the core solver is called directly, its
internal default seed is~0 unless overridden. Every reported run uses
the full benchmark pipeline: Phase~I/II, BNLP polishing, LPEC
refinement, and a final B-stationarity post-check.

\begin{table}[!htb]
\tbl{MPECSS default configuration used in all benchmark runs.}
{\begin{tabular}{@{}llll@{}}
\toprule
Parameter & Symbol & Value & Description \\
\midrule
Initial regularization   & $t_0$              & $1.0$       & Scholtes parameter at $k=0$ \\
Reduction factor         & $\kappa$           & $0.5$       & Geometric base rate \\
Complementarity tolerance& $\varepsilon_{\mathrm{tol}}$ & $10^{-6}$ & Convergence threshold \\
Stationarity tolerance   & $\tau$             & $10^{-6}$   & Sign-test tolerance \\
Maximum outer iterations & $k_{\max}$         & $3000$      & Phase~II iteration cap \\
Floor stagnation window  & ---                & $20$        & Exit if $t_k=10^{-14}$ unchanged \\
Adaptive update          & ---                & enabled     & product-residual and stagnation based $t$-control \\
Steering (sign test)     & ---                & enabled     & S-stationarity test plus Phase~III certification \\
Wall-clock limit         & $T_{\max}$         & $3600\,$s   & Per-problem timeout \\
Benchmark seed           & ---                & 42          & Benchmark-runner reproducibility \\
\bottomrule
\end{tabular}}
\label{tab:params}
\end{table}

The benchmark script writes a detailed CSV row for every instance
together with a per-run environment JSON recording the command line,
package versions, and hardware configuration. In addition to standard
convergence fields, the logger records per-iteration test metrics
(residuals, multiplier bounds, biactive counts, regime labels),
phase-level timing splits, solver fallback flags, best-point tracking,
and failure metadata. All reported times in this section are
\emph{wall-clock} times measured by the benchmark harness, not process
CPU times. Unless stated otherwise, median times in the size-stratified
tables are computed over converged instances only.
The size-class labels reported below are taken directly from the
machine-generated \texttt{problem\_size\_mode} field in the archived
CSV files: \emph{small} if $n_x < 50$, \emph{medium} if
$50 \le n_x < 500$, and \emph{large} if $n_x \ge 500$.

Each problem is assigned one of the following outcome classifications based on the solver's termination status and stationarity certificate:
(i)~\emph{B-stationary}---the solver reports \texttt{converged} and the Phase~III LICQ/LPEC verification certifies B-stationarity;
(ii)~\emph{C-stationary}---the solver reports \texttt{converged} but only C-stationarity can be certified (B-stationarity verification fails);
(iii)~\emph{Timeout}---the 3600\,s wall-clock limit is exceeded before convergence;
(iv)~\emph{Other failures}---complementarity infeasibility
(\texttt{comp\_infeasible}), NLP solver failure
(\texttt{nlp\_failure}), out-of-memory (\texttt{oom}), worker crash, or
model-loading failure. Most \emph{converged} results satisfy the target
threshold $r_{\mathrm{comp}} \leq 10^{-6}$ directly; however, in the
benchmark framework a final B-stationarity post-check may also relabel a
small number of near-feasible outcomes with
$r_{\mathrm{comp}} \leq 10\,\varepsilon_{\mathrm{tol}}$ after the final
certification stage.

Three benchmark collections are used: MPECLib, MacMPEC and NOSBENCH.


% --------------------------------------------------------------------------
\subsection{MPECLib Benchmark Results}
\label{sec:mpeclib}
% --------------------------------------------------------------------------

MPECLib~\cite{MPECLib} is a standard MPEC test set maintained by GAMS
World originally containing 92 problems drawn from bilevel optimization,
contact mechanics, game theory, and structural optimization.  Of these,
3 problems (\texttt{three}, \texttt{bard2}, and \texttt{qvi})
are excluded from our experiments as they contain modeling constructs
that fall outside the pure MPEC formulation~\eqref{eq:general_mpec}---specifically,
these problems include disjunctive constraints, general complementarity
systems, or other extensions not directly expressible as standard
complementarity pairs.  The archived raw CSV still logs these three
instances with status \texttt{unsupported\_model}; the analysis below
filters them out and therefore reports the supported 89-problem subset.
We solve these 89 problems with MPECSS using the configuration in
Table~\ref{tab:params}.


% ··· 5.2.1 Summary statistics ·············································
\subsubsection{Summary Statistics}
\label{sec:mpeclib_summary}

Table~\ref{tab:mpeclib_summary} summarises the overall outcomes.
MPECSS converged on 82 of 89 supported problems (92.1\% [85.4\% B-stationary among converged]), certifying
B-stationarity on 70 instances (78.7\%) and C-stationarity on the
remaining 12 (13.5\%).  The remaining 7 problems (7.9\%) did not converge:
4 reached the 3600\,s timeout limit, 2 terminated with stationarity
unverifiable, and 1 was classified as complementarity-infeasible.

\begin{table}[!htb]
\tbl{MPECSS outcome summary on the supported MPECLib benchmark (89 problems,
         3600\,s time limit per problem).}
{\begin{tabular}{@{}lrr@{}}
\toprule
Outcome                          & Count & Percentage \\
\midrule
Converged -- B-stationary        & 70    & 78.7\%     \\
Converged -- C-stationary        & 12    & 13.5\%     \\
\textit{Total converged}         & \textit{82} & \textit{92.1\%} \\
\addlinespace
Timeout ($>$3600\,s)             & 4     & 4.5\%      \\
Stationarity unverifiable        & 2     & 2.2\%      \\
Complementarity infeasible       & 1     & 1.1\%      \\
\addlinespace
\textbf{Total}                   & \textbf{89} & \textbf{100\%} \\
\bottomrule
\end{tabular}}
\label{tab:mpeclib_summary}
\end{table}

% Figure~\ref{fig:mpeclib_outcomes} presents the same information
% graphically.  The left panel shows the overall outcome distribution;
% the right panel breaks it down by problem size class (small: $n \leq
% 50$; medium: $50 < n \leq 500$; large: $n > 500$).
MPECSS demonstrates strong performance across all problem size categories.
All 29 small problems ($n_x < 50$) converge successfully (100\%), with all 29 achieving B-stationarity.
All 47 medium-scale instances ($50 \leq n_x < 500$) also converge successfully, with 36 certified as B-stationary and 11 as C-stationary.
For the 13 large-scale problems ($n_x \geq 500$), 6 converge successfully (46.2\%), with 5 achieving B-stationarity and 1 reaching C-stationarity.
The non-converging cases are concentrated in the \texttt{tinque}
family (4 timeout instances, with the remaining 2
instances converging) and the \texttt{frictional\_block} family
(2 stationarity-unverifiable instances and 1 complementarity-infeasible
termination). These
large-scale problems with thousands of complementarity constraints
pose significant computational challenges for the open-source MUMPS linear solver.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.6\textwidth]{mpeclib_outcomes}%
  \caption{MPECSS outcome distribution on the supported MPECLib subset (89 problems).}
  \label{fig:mpeclib_outcomes}
\end{figure}


% ··· 5.2.2 Results by size class ··········································
\subsubsection{Results by Problem Size Class}
\label{sec:mpeclib_size}

Table~\ref{tab:mpeclib_size} stratifies outcomes by problem size.
Small problems (typically 1--10 complementarity pairs) are
entirely solved with a 100\% convergence rate; all 29 achieve B-stationarity.
Medium-sized problems also achieve a 100\% convergence rate,
with 36 of 47 instances certified as B-stationary and 11 as C-stationary.
The timing results reveal that small problems solve rapidly (median 0.7\,s),
while medium problems require more substantial computation (median 80.1\,s).
Large problems ($n_x \geq 500$) present the greatest challenge: of 13 instances,
6 converge (5 to B-stationarity and 1 to C-stationarity),
while 4 timeout and 3 terminate with other failures
(2 stationarity-unverifiable, 1 complementarity-infeasible).
The non-converging cases are concentrated in the \texttt{tinque}
and \texttt{frictional\_block} families, both involving more than
1,000 complementarity pairs in their hardest instances.

\begin{table}[!htb]
\tbl{MPECLib outcomes stratified by problem size class (89 supported problems).}
{\begin{tabular}{@{}lcccccc@{}}
\toprule
Size class
  & Total
  & B-stat.
  & C-stat.
  & Timeout
  & Other
  & Median wall-clock (s) \\
\midrule
Small  ($n_x < 50$)          & 29 & 29 & 0  & 0 & 0 & 0.7 \\
Medium ($50 \leq n_x < 500$) & 47 & 36 & 11 & 0 & 0 & 80.1 \\
Large  ($n_x \geq 500$)      & 13 &  5 & 1  & 4 & 3 & 232.3 \\
\midrule
\textbf{Total}               & 89 & 70 & 12 & 4 & 3 & 36.3 \\
\bottomrule
\end{tabular}}
\label{tab:mpeclib_size}
\end{table}


% ··· 5.2.3 Results by problem family ······································
\subsubsection{Results by Problem Family}
\label{sec:mpeclib_family}

Several observations stand out from the per-family results.
\emph{Complete success} (all instances solved to
B-stationarity): \texttt{bard} (2/2), \texttt{bartruss} (6/6),
\texttt{dempe} (2/2), \texttt{desilva} (1/1), \texttt{ex9} (4/4), \texttt{find\_c} (12/12),
\texttt{fjq} (1/1), \texttt{gauvin} (1/1), \texttt{hq} (1/1),
\texttt{kehoe} (3/3), \texttt{kojshin} (2/2), \texttt{mss} (1/1),
\texttt{nappi} (4/4), \texttt{outrata} (4/4), \texttt{oz} (1/1),
and \texttt{tinloi} (1/1).
\emph{Mixed but mostly successful families:} the \texttt{find\_a}
family yields 9 B-stationary and 3 C-stationary solutions, while
\texttt{find\_b} yields 10 B-stationary and 2 C-stationary
solutions.
\emph{C-stationarity without B-stationarity certificate}:
The \texttt{aampec} family (6 instances, all with 69 complementarity pairs)
converges to C-stationary points on all instances; the high complementarity
density ($q/n \approx 0.96$) presents challenges for B-stationarity certification.
\emph{Non-converging cases}: The \texttt{tinque} family (6 large-scale
instances with 3,000--5,700 variables from contact-mechanics discretisations)
has
4 timeouts and 2 converged cases (1 B-stationary and 1 C-stationary).
The \texttt{frictional\_block} family (6 instances) has
2 stationarity-unverifiable terminations, 1 complementarity-infeasible
termination, and 3 converged instances; these are known
computationally intensive problems whose cost is dominated by sparse
linear algebra.


% ··· 5.2.6 Discussion of failures ·········································
\subsubsection{Discussion of Non-Converging Cases and C-Stationarity}
\label{sec:discussion_large}

The \texttt{tinque} instances are large-scale problems (4,000--5,700
variables, 2,900--4,500 complementarity pairs) arising from
contact-mechanics discretisations. Four of the six supported instances
exceed the 3600\,s time limit, while the remaining two converge.
These problems represent the computational frontier for regularisation-based
MPEC solvers using open-source linear algebra; stronger sparse-direct or
parallel sparse-factorisation backends would likely help on this family.

The \texttt{frictionalblock} instances range from 682 to 2,855 variables
with 332 to 1,380 complementarity pairs.  Two instances terminate with
stationarity unverifiable and one is classified as complementarity-infeasible,
while the remaining three converge to B-stationary points.  The
non-converged outcomes indicate numerical difficulty in the final
stationarity and feasibility certification steps for this family.

The six \texttt{aampec} problems have $n_x = 72$ variables and $q = 69$
complementarity pairs, giving an extremely high complementarity
density ($q/n \approx 0.96$).  All six converge to C-stationarity.
The high complementarity density
makes B-stationarity certification particularly challenging, as the
biactive set tends to be large at feasible points.

The 92.1\% convergence rate [85.4\% B-stationary among converged] on the 89-problem supported MPECLib subset
demonstrate the effectiveness of MPECSS across a diverse problem collection.
The 7 non-converging cases are concentrated in very large-scale problems ($>$1,000
complementarity pairs) that push the limits of the open-source MUMPS solver.
The 12 C-stationary solutions, predominantly from \texttt{aampec} and
the \texttt{find\_*} families, represent problems where the LPEC post-processing
correctly identifies that stronger stationarity certificates cannot be
guaranteed.


% --------------------------------------------------------------------------
\subsection{MacMPEC Benchmark Results}
\label{sec:macmpec}
% --------------------------------------------------------------------------

MacMPEC~\cite{Leyffer2000} is an early and widely used MPEC
benchmark collection, comprising 191 problems drawn from bilevel
optimisation, economic equilibrium modelling, engineering design, and
game theory.  We use the 191-problem version as distributed on the
AMPL/MacMPEC repository, which includes problems added after the
original 2000 release.  All experiments follow the same methodology
described in Section~\ref{sec:exp_setup} with the parameter settings
in Table~\ref{tab:params}.

% ··· 5.3.1 Summary statistics ·············································
\subsubsection{Summary Statistics}
\label{sec:macmpec_summary}

Table~\ref{tab:macmpec_summary} summarises the overall outcomes.
MPECSS converged on 184 of 191 problems (96.3\% [87.5\% B-stationary among converged]), certifying
B-stationarity on 161 instances (84.3\%) and C-stationarity on the
remaining 23 (12.0\%).  Six problems (3.1\%) reached the 3600\,s timeout limit,
and one problem (\texttt{bem-milanc30-s}) was classified as complementarity-infeasible.

\begin{table}[!htb]
\tbl{MPECSS outcome summary on the MacMPEC benchmark (191 problems,
         3600\,s time limit per problem).}
{\begin{tabular}{@{}lrr@{}}
\toprule
Outcome                          & Count & Percentage \\
\midrule
Converged -- B-stationary        & 161   & 84.3\%     \\
Converged -- C-stationary        & 23    & 12.0\%     \\
\textit{Total converged}         & \textit{184} & \textit{96.3\%} \\
\addlinespace
Timeout ($>$3600\,s)             & 6     & 3.1\%      \\
Complementarity infeasible       & 1     & 0.5\%      \\
\addlinespace
\textbf{Total}                   & \textbf{191} & \textbf{100\%} \\
\bottomrule
\end{tabular}}
\label{tab:macmpec_summary}
\end{table}

MPECSS demonstrates strong performance across all problem size categories.
All 96 small problems ($n_x < 50$) converge successfully (100\%), with 82 achieving B-stationarity.
For the 71 medium-scale instances ($50 \leq n_x < 500$), 70 converge
(98.6\%), with 62 certified as B-stationary and 8 as C-stationary.
For the 24 large-scale problems ($n_x \geq 500$), 18 converge successfully (75.0\%), with 17 achieving B-stationarity and 1 reaching C-stationarity.
The 7 non-converging cases include 6 timeouts from the \texttt{siouxfls}, \texttt{incid-set}, and \texttt{qpec} families
and one complementarity-infeasible instance.

\begin{figure}[!htb]
  \centering
  \includegraphics[width=0.6\textwidth]{macmpec_outcomes}%
  \caption{MPECSS outcome distribution on MacMPEC.}
  \label{fig:macmpec_outcomes}
\end{figure}


% ··· 5.3.2 Results by size class ··········································
\subsubsection{Results by Problem Size Class}
\label{sec:macmpec_size}

Table~\ref{tab:macmpec_size} stratifies outcomes by problem size.
Small problems ($n_x < 50$, typically 1--20 complementarity pairs) achieve
a perfect 100\% convergence rate across all 96 instances, with 82 achieving
B-stationarity and 14 achieving C-stationarity.
Medium-sized problems ($50 \leq n_x < 500$) achieve 98.6\% convergence
across 71 instances, with 62 instances (87.3\%) achieving B-stationarity
and 8 (11.3\%) achieving C-stationarity.
The wall-clock timing results reveal that small problems solve rapidly (median 0.8\,s),
while medium problems require moderate computation (median 7.5\,s).
Large problems ($n_x \geq 500$) present the greatest challenge: of 24 instances,
18 converge (75.0\%), with 17 achieving B-stationarity and 1 achieving C-stationarity.
The 6 timeout cases are concentrated in the \texttt{siouxfls}, \texttt{incid-set},
and \texttt{qpec} families, while \texttt{bem-milanc30-s} is the lone
complementarity-infeasible instance.

\begin{table}[!htb]
\tbl{MacMPEC outcomes stratified by problem size class.}
{\begin{tabular}{@{}lcccccc@{}}
\toprule
Size class
  & Total
  & B-stat.
  & C-stat.
  & Timeout
  & Other
  & Median wall-clock (s) \\
\midrule
Small  ($n_x < 50$)          & 96 & 82 & 14 & 0 & 0 & 0.8 \\
Medium ($50 \leq n_x < 500$) & 71 & 62 & 8  & 1 & 0 & 7.5 \\
Large  ($n_x \geq 500$)      & 24 & 17 & 1  & 5 & 1 & 781.4 \\
\midrule
\textbf{Total}               & 191 & 161 & 23 & 6 & 1 & 3.2 \\
\bottomrule
\end{tabular}}
\label{tab:macmpec_size}
\end{table}


% ··· 5.3.3 Results by problem family ······································
\subsubsection{Results by Problem Family}
\label{sec:macmpec_family}

Several observations stand out from the per-family results.
Because the archived CSV stores the family field as the generic label
\texttt{macmpec}, the family counts in this paragraph are reconstructed
from the standard filename prefixes used in the MacMPEC distribution.
\emph{Near-complete B-stationarity} is observed for \texttt{pack}
(44/45 B-stat, 1 C-stat), \texttt{gnash} (17/20 B-stat, 3 C-stat),
\texttt{ex} (17/19 B-stat, 2 C-stat), \texttt{bard} (5/6 B-stat,
1 C-stat), and \texttt{design} (5/6 B-stat, 1 C-stat).
\emph{C-stationarity without a strong certificate:} the \texttt{qpec}
family (10 instances) converges on 9 instances, with only 1 achieving
B-stationarity and 8 achieving C-stationarity; the \texttt{bilevel}
family (5 instances) converges on all 5, with 3 B-stationary and
2 C-stationary outcomes.
\emph{Timeout cases}: the \texttt{siouxfls} family (2 large-scale
instances with 2,403 variables and 1,748 complementarity pairs) times
out on both instances. The \texttt{incid-set} family contributes three
additional timeout cases, and one \texttt{qpec} instance also times out.
The named pair \texttt{ralph1}
and \texttt{ralph2} both converge to B-stationary points, while
\texttt{ralphmod} is a separate converged C-stationary instance.


% ··· 5.3.4 Discussion of failures ·········································
\subsubsection{Discussion of Timeout and Failure Cases}
\label{sec:macmpec_failures}

The six timeout problems include:
two \texttt{siouxfls} traffic network instances (\texttt{siouxfls} and \texttt{siouxfls1})
with 2,403 variables and 1,748 complementarity pairs each,
three \texttt{incid-set} instances
(\texttt{incid-set1-32}, \texttt{incid-set-32}, and \texttt{incid-set2c-32})
with up to 1,989 variables and 961 complementarity pairs, and one
\texttt{qpec} instance (\texttt{qpec-200-4}) with 240 variables and
200 complementarity pairs.
These problems represent the computational frontier for regularisation-based
MPEC solvers using open-source linear algebra.

The single complementarity-infeasible case (\texttt{bem-milanc30-s}) is a
large boundary element method problem with 3,436 variables and 1,464
complementarity pairs.  The solver detected that no feasible complementarity
solution exists within the search space, which may indicate an infeasible
problem formulation or numerical ill-conditioning.

The 96.3\% convergence rate [87.5\% B-stationary among converged] on MacMPEC demonstrates the robustness of MPECSS across diverse problem origins.  The 7 non-converging cases
are concentrated in large-scale problems that push the limits of the open-source
MUMPS linear solver.

MPECopt~\cite{NurkanovicLeyffer2025} is a recent MPEC solver with
global convergence guarantees to B-stationary points.  A detailed
comparison on the MacMPEC collection is provided in Section~\ref{sec:perf_profile}.
The comparison focuses on (i)~convergence rates, (ii)~B-stationarity
certification rates, and (iii)~wall-clock time.  Note that MPECopt
requires MATLAB and Gurobi, whereas MPECSS is fully open-source.


% --------------------------------------------------------------------------
\subsection{NOSBENCH Benchmark Results}
\label{sec:nosbench}
% --------------------------------------------------------------------------

NOSBENCH~\cite{NurkanovicPozharskiyDiehl2024} is a recently introduced
benchmark collection comprising 603 problems arising from optimal
control of nonsmooth dynamical systems.  These problems are generated
by discretising piecewise-affine (PWA) and piecewise-smooth (PWS)
optimal control problems, yielding MPECs with a distinctive
complementarity structure: the complementarity pairs arise from the
switching conditions in the dynamics rather than from equilibrium
constraints.  Problem sizes range from small (under 50 variables) to
medium-scale (several hundred variables), with complementarity counts
typically proportional to the discretisation horizon.  All experiments
follow the same methodology described in Section~\ref{sec:exp_setup}
with the parameter settings in Table~\ref{tab:params}.  The repository
contains four archived official NOSBENCH group CSVs together with their
matching run-environment JSON files; however, the current archived bundle
still reflects a benchmark-export race between synthetic timeout/crash
bookkeeping and late worker results.  As a consequence, the checked-in
CSV bundle is retained for auditability but not yet frozen as the
publication source of truth for NOSBENCH headline counts.

% ··· 5.4.1 Summary statistics ·············································
\subsubsection{Summary Statistics}
\label{sec:nosbench_summary}

Table~\ref{tab:nosbench_summary} records the archival status of the
current checked-in NOSBENCH bundle.  Across the four official group
CSVs, the repository contains 610 terminal rows for 603 unique problem
names.  The corresponding run-environment JSON files also expose the
same mismatch through their \texttt{problem\_count} and
\texttt{result\_count} fields (151/153, 151/153, 151/152, and 150/152
for groups 1--4).  Manual inspection shows that the duplicated rows are
concentrated in seven problems and arise from timeout/crash or
timeout/late-result pairs.  Because these duplicated terminal rows would
contaminate any publication table, the NOSBENCH manuscript numbers are
left as placeholders until the full suite is regenerated from the
corrected exporter.

\begin{table}[!htb]
\tbl{Archival status of the checked-in NOSBENCH official bundle.}
{\begin{tabular}{@{}ll@{}}
\toprule
Item & Value \\
\midrule
Official group CSV files & 4 \\
Unique problem names across the bundle & 603 \\
Continued [to be completed] \\
% Terminal rows currently present in the archived CSVs & 610 \\
% Known duplicated problems requiring a refresh & 7 \\
% Publication-ready NOSBENCH headline table & pending corrected re-export \\
\bottomrule
\end{tabular}}
\label{tab:nosbench_summary}
\end{table}

The duplicate-row issue is now fixed in the benchmark exporter in the
current code base; the remaining task for NOSBENCH is therefore a clean
full-suite refresh rather than an algorithmic redesign.  Until that
refresh is completed, we keep the NOSBENCH section as an explicit
publication placeholder instead of reporting stale percentages from an
older manuscript draft.


% ··· 5.4.2 Results by size class ··········································
\subsubsection{Results by Problem Size Class}
\label{sec:nosbench_size}

Table~\ref{tab:nosbench_size} is intentionally left as a manuscript
placeholder.  The size-stratified publication table will be regenerated
from the corrected archive so that each NOSBENCH problem contributes a
single terminal row before the counts are broken down by
\texttt{problem\_size\_mode}.

\begin{table}[!htb]
\tbl{NOSBENCH size-stratified publication placeholder.}
{\begin{tabular}{@{}ll@{}}
\toprule
Item & Status \\
\midrule
Small / medium / large outcome counts & to be regenerated from corrected CSVs \\
Median wall-clock summaries & to be regenerated from corrected CSVs \\
Reason for placeholder & current archive contains duplicate timeout/crash rows \\
\bottomrule
\end{tabular}}
\label{tab:nosbench_size}
\end{table}


% ··· 5.4.3 Results by problem family ······································
\subsubsection{Results by Problem Family}
\label{sec:nosbench_family}

The family-level publication discussion is likewise postponed until the
corrected archive is available.  The current checked-in bundle is still
useful diagnostically: the duplicated rows are concentrated in a small
set of families (\texttt{3CPCLS}, \texttt{CARHYS}, and
\texttt{DAOBCLS}), which is consistent with the timeout/crash export
race identified in the benchmark harness.

% ··· 5.4.4 Discussion of failures ·········································
\subsubsection{Discussion of Timeout and Failure Cases}
\label{sec:nosbench_failures}

The duplicated examples in the archived CSVs are all failure-adjacent:
timeout/crash pairs for four \texttt{3CPCLS} and \texttt{CARHYS}
instances, plus a timeout/late-result pair for one \texttt{DAOBCLS}
instance.  This is precisely the class of ambiguity that the exporter
fix in the current revision removes.  Once the NOSBENCH suite is
rerun or the archived bundle is re-exported from the corrected harness,
the summary, size-stratified, and family-level tables in this section
should be regenerated directly from that corrected bundle before final
publication.


% --------------------------------------------------------------------------
\subsection{Performance Profile Analysis}
\label{sec:perf_profile}
% --------------------------------------------------------------------------

Performance profiles, introduced by Dolan and Mor\'{e}~\cite{DolanMore2002},
provide a standardised methodology for comparing optimisation solvers
across a benchmark suite.  For each problem~$p$ and solver~$s$, let
$t_{p,s}$ denote the performance metric (e.g., complementarity residual
or wall-clock time).  The performance ratio is defined as
\begin{equation*}
  r_{p,s} = \frac{t_{p,s}}{\min_s t_{p,s}},
\end{equation*}
and the performance profile $\rho_s(\tau)$ gives the fraction of
problems for which solver~$s$ achieves a performance ratio at most~$\tau$:
\begin{equation*}
  \rho_s(\tau) = \frac{|\{p : r_{p,s} \leq \tau\}|}{|\mathcal{P}|},
\end{equation*}
where $\mathcal{P}$ is the set of all benchmark problems.  A solver
with a higher $\rho_s(\tau)$ for all $\tau$ is uniformly better.

A direct MPECSS--MPECopt comparison on MacMPEC will be inserted once
the curated side-by-side result bundle and the corresponding profile
figure are frozen. The intended comparison metric is the final
complementarity residual $r_{\mathrm{comp}}$, with a problem counted as
``solved'' only when $r_{\mathrm{comp}} \leq 10^{-6}$ and the final
certificate is B-stationarity; unsolved problems are assigned
$r_{p,s} = \infty$.

% [Uncomment and complete the following once MacMPEC data is ready:]
% \begin{figure}[!htb]
%   \centering
%   \includegraphics[width=0.72\textwidth]{perf_profile_macmpec}
%   \caption{Dolan--Mor\'{e} performance profiles on MacMPEC (191 problems),
%     comparing MPECSS with MPECopt~\cite{NurkanovicLeyffer2025}.
%     The $y$-axis gives the fraction of problems solved within a factor
%     $\tau$ of the best complementarity residual; higher is better.
%     At $\tau = 1$, the profile value indicates the fraction of problems
%     on which each solver achieves the best result.}
%   \label{fig:perf_profile}
% \end{figure}
% Define shorthand for scientific notation
\newcommand{\e}[1]{\times 10^{#1}}


\subsection{Performance and Benchmark Comparison}
\label{sec:aggregate_stats}

Table \ref{tab:aggregate_results} summarizes overall performance across the three benchmark suites.

\begin{table}[htb]
\tbl{Frozen archived benchmark summary for the current manuscript bundle.}
{\begin{tabular}{l r r r r r r}
\toprule
\textbf{Benchmark} & \textbf{Total} & \textbf{Converged} & \textbf{Convergence} & \textbf{Avg wall-clock} &
\textbf{Avg Compl.} & \textbf{B-stat} \\
 & \textbf{Probs} & (\%) & \textbf{Rate} & \textbf{Time (s)} & \textbf{Viol.} & \textbf{Verified (\%)} \\
\midrule
MacMPEC & 191 & 184 & 96.3\% & 166.5 & $2.2\e{-8}$ & 87.5\% \\
MPECLib & 89 & 82 & 92.1\% & 253.1 & $4.1\e{-7}$ & 85.4\% \\
NOSBENCH & 603 & TBD & TBD & TBD & TBD & TBD \\
\midrule
\textbf{Frozen subtotal} & 280 & 266 & 95.0\% & 193.2 & $1.4\e{-7}$ & 86.8\% \\
\bottomrule
\end{tabular}}
\label{tab:aggregate_results}
\end{table}

Cross-suite difficulty stratification is deferred to the final archived
benchmark bundle, where the size-class definitions and family metadata
will be regenerated from a single machine-produced summary rather than
manually assembled across suites.  In the present manuscript revision,
MacMPEC and MPECLib are the two frozen suites, while NOSBENCH remains a
placeholder pending corrected full-suite regeneration.
